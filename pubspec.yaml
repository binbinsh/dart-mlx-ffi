name: dart_inference
description: Zig-backed Dart FFI inference runtime for Core ML, ONNX Runtime, LiteRT, and MLX backends
version: 1.2026.44
homepage: https://github.com/binbinsh/dart-inference
repository: https://github.com/binbinsh/dart-inference
issue_tracker: https://github.com/binbinsh/dart-inference/issues

topics:
  - ffi
  - machine-learning
  - inference

platforms:
  android:
  ios:
  linux:
  macos:
  windows:

environment:
  sdk: ^3.11.1

dependencies:
  archive: ^4.0.7
  code_assets: ^1.0.0
  ffi: ^2.1.4
  hooks: ^1.0.0
  logging: ^1.3.0
  path: ^1.9.1

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.28.0
