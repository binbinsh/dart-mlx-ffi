name: dart_mlx_ffi
description: Dart FFI model runtime package for MLX, Core ML, ONNX Runtime, and LiteRT
version: 26.414.19
homepage: https://github.com/binbinsh/dart-mlx-ffi
repository: https://github.com/binbinsh/dart-mlx-ffi
issue_tracker: https://github.com/binbinsh/dart-mlx-ffi/issues

topics:
  - ffi
  - machine-learning
  - mlx

platforms:
  android:
  ios:
  linux:
  macos:
  windows:

environment:
  sdk: ^3.11.1

dependencies:
  archive: ^4.0.7
  code_assets: ^1.0.0
  ffi: ^2.1.4
  hooks: ^1.0.0
  logging: ^1.3.0
  path: ^1.9.1

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
