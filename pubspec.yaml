name: dart_mlx_ffi
description: Production-ready Dart FFI bindings for Apple MLX on macOS
version: 26.310.2051
homepage: https://github.com/binbinsh/dart-mlx-ffi
repository: https://github.com/binbinsh/dart-mlx-ffi
issue_tracker: https://github.com/binbinsh/dart-mlx-ffi/issues

topics:
  - ffi
  - machine-learning
  - apple-silicon
  - mlx

platforms:
  macos:

environment:
  sdk: ^3.11.1

dependencies:
  archive: ^4.0.7
  code_assets: ^1.0.0
  ffi: ^2.1.4
  hooks: ^1.0.0
  logging: ^1.3.0

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
