name: dart_mlx_ffi
description: Production-ready Dart FFI package for Apple MLX runtimes on iOS and macOS
version: 26.405.15
homepage: https://github.com/binbinsh/dart-mlx-ffi
repository: https://github.com/binbinsh/dart-mlx-ffi
issue_tracker: https://github.com/binbinsh/dart-mlx-ffi/issues

topics:
  - ffi
  - machine-learning
  - mlx

platforms:
  ios:
  macos:

environment:
  sdk: ^3.11.1

dependencies:
  archive: ^4.0.7
  code_assets: ^1.0.0
  ffi: ^2.1.4
  hooks: ^1.0.0
  logging: ^1.3.0
  path: ^1.9.1

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
