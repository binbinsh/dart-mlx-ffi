name: dart_inference
description: Private Dart FFI runtime module for Grid Heap AI Runtime
version: 1.2026.159
publish_to: none
homepage: https://airuntime.dev
repository: https://github.com/gridheap-inc/airuntime-dev
issue_tracker: https://github.com/gridheap-inc/airuntime-dev/issues

topics:
  - ffi
  - machine-learning
  - inference

platforms:
  android:
  ios:
  linux:
  macos:
  windows:

environment:
  sdk: ^3.11.1

dependencies:
  archive: ^4.0.7
  code_assets: ^1.0.0
  ffi: ^2.1.4
  gridheap_model_contracts:
    path: ../../../models/shared/dart_contracts
  hooks: ^1.0.0
  logging: ^1.3.0
  path: ^1.9.1

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.28.0
