{
  "name": "search_notes",
  "description": "Search local notes for a concise answer.",
  "parameters": {
    "type": "object",
    "required": ["query"],
    "properties": {
      "query": {
        "type": "string"
      }
    }
  }
}
