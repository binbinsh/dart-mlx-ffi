# AGENTS.md

- Do not modify vendored source code in this directory unless the user explicitly requests a vendor update; keep local integration changes in project-owned build files, wrappers, or runtime code outside `vendors/`.
